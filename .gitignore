# Secrets and credentials — never committed (CLAUDE.md, RULE on credentials)
.env
.env.*
*.key
*.pem

# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/

# OS
.DS_Store

# Data exports — personal data never leaves this system via the repo
*.parquet
*.pq
/exports/
/data/exports/

# Local, uncommitted staging for the legacy archive (Phase 0). Never committed
# until OQ-01 (credential rotated) and OQ-03 (repo public/private) are settled.
_legacy_snapshot/
# Supabase CLI scratch state (reviewer session 19 #15)
supabase/.temp/

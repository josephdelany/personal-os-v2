# DECISIONS — ADR index

One line per decision. Read the full ADR before changing anything it covers.
ADRs are immutable: a superseded decision is marked superseded and kept, never
edited and never deleted, because the reason it was made is the thing worth
preserving.

| ID | Status | Date | Decision |
|---|---|---|---|
| ADR-0001 | Accepted | 2026-08-06 | Compute placement — three tiers, one owner per number, model plans and narrates but never computes |
| ADR-0002 | Accepted | 2026-08-06 | The atom — bitemporal, three-valued presence, interval-valued, `state_class` in the schema |
| ADR-0004 | Accepted | 2026-08-23 | Entity/link **table shape only** — bitemporal, provenance, `corrected_by_human` (RULE-10); `entity_type` open TEXT pending the ontology spec (OQ-16); in `core` schema (never touches old `public.entities`). Resolution algorithm (blocking keys, thresholds, review queue) deferred to Phase 4 |
| ADR-0005 | Stub | 2026-08-15 | Nutrition resolution — **stub**: records only the `weighed` interval width (±10%, equal to `labelled`, provisional; distinct method kept for calibration). Cache-first lookup, portion table, and remaining widths still to be authored before Phase 3 |
| ADR-0007 | Accepted | 2026-08-23 | Multiplicity — store native **e-values** (`e_value`, `e_value_method`, `e_process_params`, `family_size`) as the family-wide FDR currency; **e-BH replaces BH-on-p** (amends RULE-21 + REQ-INF-106). A **power-for-anytime-validity trade** (Joe peeks constantly), not a strict upgrade. Corrections: p→e conversion *is* possible but lossy; arXiv:2502.08539 is cautionary. Compute is Phase 6; `online-fdr` needs a RULE-28 dep-ADR |
| ADR-0008 | Accepted | 2026-08-23 | Capture **schema** — extended `capture_source` enum (spec's 4 + `notification_parse` + reserved `healthkit_workout`/`email_receipt`/`location`); workout dedup identity `(start-window, duration-window)` + source **tiebreaker** (not source-first); Apple on-device context **4,096** tokens. Transport contract deferred to Phase 3. OQ-18 collision flagged (no workout data exists yet) |
| ADR-0010 | Accepted | 2026-08-23 | RULE-02 enforcement — append-only by **grant REVOKE** (app roles) **and trigger** (catches even the owner, whose implicit grant is unrevocable). The literal RULE-02 CI query counts the owner self-grant so can never return 0; the CI check is **scoped to app roles** per REQ-CAP-012 — a correction, not a weakening (RULE-00), **flagged for Joe's ratification + a constitution-text update**. Behavioural proof needs no fabricated row (permission denied on empty table) |
| ADR-0011 | Accepted | 2026-08-23 | `features.json` write-lock — agent denied Edit/Write on the ledger at the permission level so it cannot mark its own work passing (verified: an Edit on the file is rejected by the permission engine). The sanctioned writer that flips an entry only on proven pytest output is deferred to Phase 3, authored with the first proving test |
| ADR-0012 | Accepted | 2026-08-23 | ETL database TLS posture — all DB access via `lib/db.py`, TLS verified against the pinned `Supabase Root 2021 CA` (`lib/certs/`, proven to anchor the live chain), `CERT_REQUIRED` + hostname check, only `VERIFY_X509_STRICT` cleared (Supabase *intermediate/leaf* omit `keyUsage`; the root carries it). CERT_NONE considered and rejected. Credential from env only. Pinned root expires 2031-04-26 |
| ADR-0013 | Accepted | 2026-08-23 | Repository is **public** (unmetered Actions for the statistical layer) named `personal-os`; resolves OQ-03/OQ-02. No personal data ever tracked in git — every data path gitignored by default, a tracked `.parquet`/`.csv`/`.db`/`.sqlite` fails CI (RULE-29 strengthened, no new rule; cap held). Dead credential scrubbed from all history 2026-08-23 via git filter-repo, verified absent from every object (see ADR-0013 addendum; all commit hashes changed as a result) |
| ADR-0014 | Accepted | 2026-08-23 | Phase-1 research corrections — RULE-11 PHIA numbers refined to 22%→74%→84% (Gemini 1.0 Ultra main results, unreplicated; conclusion unchanged); RULE-17 causal-discovery distrust strengthened with CausalDynamics (PCMCI+ **at chance** on simple systems, AUROC ~0.52); **new RULE-13** "the model never selects the temporal specification" (HEARTS, ICML 2026), retiring the former RULE-13 into RULE-14 to hold the 30-cap. *Citations re-verified 2026-08-23, see addendum.* |
| ADR-0015 | Accepted | 2026-08-23 | Phase-1 constitution ratification (Gate 1) — all 31 rules reviewed and **kept**; reversals RULE-18/23/30 affirmed explicitly; RULE-19 amended (one-time early exploratory pre-registration pass, waiting clock starts day one); RULE-30 amended (revisit trigger if iOS/WebKit ships persistent PWA media grants; rationale strengthened — on-device Foundation Model + SpeechTranscriber (wins on *accuracy*) make Shortcuts capture free/private/offline/better). *WebKit 215884 citation removed as a misattribution 2026-08-23.* |
| ADR-0016 | Accepted | 2026-08-23 | Analytical store — **Postgres authoritative** (`atoms`/`raw_captures`/`findings`/`ops.*`); Parquet-on-R2 the analytical mirror + scratch, DuckDB in the Actions runner (hosted Supabase cannot run pg_duckdb/pg_ducklake); results written back with provenance (INV-1/RULE-12 stay in one enforceable store). **No R2 code this phase.** R2 Class-A ops are the real $0 leak → batch/compact writes. References **OQ-20** (500 MB fill; append-only means no delete escape hatch) |
| ADR-0017 | Accepted | 2026-08-23 | Non-response is a row — `core.prompt_dispatch`, one row per prompt **issued**; `response_state ∈ {pending, answered, seen_declined, delivered_unseen, partial, expired}`; `never_scheduled` = absence of a row. Distinct axis from RULE-07 presence. JMIR e65350 **snippet-confirmed, primary unreachable**; decision stands on its own reasoning. Wired Phase 3+ |
| ADR-0018 | Accepted | 2026-08-23 | Metric-registry scale/rounding metadata — `self_report`, `response_scale`, `n_scale_points`, `rounding_step` per variable, so self-reports are stored as coarsened interval data (arXiv:2501.10726, a GMM paper; schema-as-metadata is our extrapolation, not the paper's claim) |
| ADR-0019 | Accepted | 2026-08-23 | Temporal amendment to ADR-0002 — instant + `time_precision` for points, `valid_interval tstzrange` for durational atoms; `subject_day` moves from *generated* to application-computed **stored** with `rule_version` (04:00 local, by start instant, **sleep by wake day**). Amends RULE-03. Resolves OQ-06. **Implementation correction, ratified by Joe 2026-08-23: transaction time is `recorded_at` (system-set at INSERT via trigger) + currency DERIVED from `supersedes` (`atoms_current` view), NOT a stored `expired_at` — a stored expiry needs an UPDATE that INV-2 forbids; the invariant wins.** |
| ADR-0020 | Accepted | 2026-08-23 | Trust/egress — `trust_level ∈ {trusted, untrusted}` on `atoms`/`raw_captures` at ingest; a session that reads personal rows holds no egress capability (and vice-versa); no generic `execute_sql` reaches a model. RULE-29 clarification, no new numbered rule (30-cap holds). NSA CSI **snippet-confirmed, primary unreachable**; decision rests on the verified lethal-trifecta + OWASP LLM01/LLM06 |
| ADR-0021 | Accepted | 2026-08-23 | Distributional forecasts — continuous predictions store `forecast_distribution` (quantiles/samples) + `dist_family`/`issued_at`/`horizon`/`crps`/`log_score_cont`; binary path (`p_forecast`/`brier`/`log_score`) kept (log score is infinite for a point mass, so no unified column). Extends RULE-20. Split from ADR-0016. Scoring compute is Phase 6 |
| ADR-0022 | Accepted | 2026-08-23 | RULE-01 clarification — the **rolled-back constraint probe**: a behavioural test MAY create a disposable schema (never `core`/`public`), INSERT fixture rows, assert, and roll back the whole transaction so nothing commits and no row is read as data. Proves INSERT-path constraints (value/presence/lane CHECKs, `force_recorded_at`, predictions XOR, prereg-freeze happy path) that a rejection on an empty table cannot reach. A clarification, not a weakening (RULE-00): nothing persists. Resolves OQ-21. RULE-01 + CLAUDE.md `<no_fabrication>` text updated the same day |
| ADR-0023 | Accepted | 2026-08-23 | Ontology taxonomy (REQ-ONT) — closed `atoms.kind` (19 members) and `entities.entity_type` (6 members) **derived** from the 34 archived tables + cited specs (7 kinds spec/ADR-cited, 12 archive-derived), enforced by **CHECK, not native ENUM** (a growing set; CHECK extension is a cheap forward migration, `ALTER TYPE` is not). `kind` is coarse; the specific measure lives in `metric_key` (registry), not the enum. Migration 0014 adds the CHECKs over the empty tables. Guesses recorded in-ADR. Resolves OQ-23; partially resolves OQ-16 (REQ-ONT half; REQ-NFR still dangling) |
| ADR-0024 | Accepted | 2026-08-23 | Reliability keepalives (REQ-NFR-001..004) — one **daily** GitHub-Actions job covers both clocks: daily Supabase write (7-day pause) + a **daily** stale-check that commits a heartbeat only when the last commit is >50 days old (bounds staleness at 51d < GitHub's 60d disable; a monthly check would allow 81d — reviewer B1). `ops.runs`/`job_registry`/`egress_log` already existed (migration 0011) — **not recreated**; both jobs registered. **ops.runs heartbeat doctrine (Joe's RULE-01 ruling):** a true "job ran at T" row (`rows_written=0`) is operational fact, not fabricated data; mechanism proven by 6 named tests (ADR-0022 rolled-back probe). **No clock runs until the repo is pushed (OQ-02) + `SUPABASE_DB_URL` secret set — F-014/F-015 stay failing.** Closes the REQ-NFR half of OQ-16. Pre-fix live rows (2 ops.runs + the keepalive_github registry row) corrected with Joe's consent (OQ-28): ops.runs marked `manual_smoke`, registry moved to the daily design. |
| ADR-0025 | Accepted | 2026-08-23 | Gate-2 backfill = **reconciliation, not row-count equality** — the 810,933-row / 63-table archive cannot map 1:1 to atoms (18 derived tables = ~111k rows would fabricate lineage, INV-1/INV-5/RULE-01; 4 duplicate tables = ~72k double-count; `intraday` fans out multi-metric). Gate 2 passes when **every archived row is mapped-to-atom OR excluded-with-a-recorded-reason**, exact per source table (`tools/backfill_map.py`, `docs/PHASE2_BACKFILL_PLAN.md`). Amends ROADMAP Gate 2 wording (stricter, not weaker — RULE-00). Plan only; no backfill executed |
| ADR-0028 | Accepted | 2026-08-23 | **Legacy is Parquet-authoritative; Postgres load deferred (option c)** — the backfill is built + **DB-verified** (309,826 atoms into a rolled-back copy, all constraints/invariants pass, reconciles Δ=0) but **committed to `core`: none**. Measured 113 MB would push the DB 200.5→313.7 MB (63%) and **triple-store** the still-live `public.intraday` (94 MB, OQ-17) for a consumer that doesn't exist until Phase 5/6; RULE-02 makes it irreversible. Legacy stays in the sha256 Parquet archive; analytical reads via DuckDB-over-Parquet (ADR-0016); `tools/backfill_run.py` is the proven re-runnable loader, held. **Gate 2 satisfied by reconciliation with load DEFERRED** (same pattern as RULE-04/OQ-22). Load trigger + sizing = OQ-29 |
| ADR-0027 | Accepted | 2026-08-23 | Backfill metric registry + per-stream modeling — `metric_registry` populated **fresh** (26 rows, units from source, `family` for the RULE-21 tree, `state_class='measurement'`; `plausible_low/high` **NULL** per RULE-06/OQ-10; `checkins` self-reports coarsened per ADR-0018). `subject_day` tz = **America/New_York** (evidenced: health offsets + youtube ET alignment; `pos__chrome` is UTC, `pos__youtube` is ET — per-stream normalization). `provenance='extracted'`, `estimate_method='measured'`, `recorded_at`=backfill instant (new-system transaction time). `trust_level` untrusted where identity rests on a third-party string (web/media/txn). Union-dedup keys per stream + **`evidence_span` carries the source** (rider 2). Deferred/flagged: `locations` (RULE-29→Phase 4), `env_db/headphone_db` may be daily aggregates, sleep-stage in `evidence_span`. Extends ADR-0018/0026 |
| ADR-0026 | Accepted | 2026-08-23 | Legacy-backfill lineage (**A′**) + gait classification — INV-1 for legacy atoms = **one `raw_captures` per source table-load** (`source='legacy_archive'`, migration 0015; payload = manifest sha256/count), **not** per-row (~24 vs ~625k rows; OQ-20 ceiling). Each atom carries its **source-row locator in `evidence_span`**, so row-level audit runs `atom → row-key → sha256-pinned Parquet → source row`. Main benefit beyond storage: a wrong kind/metric boundary is fixable by **re-derivation** from the archive, not re-backfill (Joe). Gait 5 series = **`activity_sample`** (movement), `walking_hr` stays `vital_sample`; settles O-Q2. **Flags a new execution blocker: the backfill map double-counts `intraday`↔`health__*` (same Apple-Health export, value-level confirmed) and `events`↔`pos__*` at ~450k scale; `metric_registry` empty (hard FK, not mappable from legacy `metric_catalog`); `locations` unfittable (RULE-29 + no coord columns) — rulings owed before any atom is written** |

| ADR-0029 | Accepted | 2026-08-23 | **Constitution split into INTEGRITY / SCOPE / HYBRID; SCOPE layer re-derived from the goal.** An audit (triggered by RULE-29 having deleted the whole location domain by conflating *storage* with *exposure*) found ~8–9 SCOPE conflicts from one root cause: the rules were ratified in Phase 1 by reading one-line summaries, not consequences. Fix is **not** immutability (that would freeze mistakes) but **amendment-by-consequence**: SCOPE needs a written consequence-analysis; INTEGRITY needs that **plus an adversarial review that tries to break the change**. Ratified rule-by-rule at consequence level. RULE-29 OPEN (store coords restricted, derive labels+mobility, egress lint — privacy intent unchanged; ADR-0013/ADR-0020 integrity clauses retained); pre-registration cluster (RULE-13/17/19) SPLIT (confirmation stays frozen; exploration continuous + displayable-as-EXPLORATORY, **tier-labelling surface built+proven first**); RULE-25/23/24 reworded; RULE-06/22/26 clarified; RULE-28 ($0) unchanged. Static coordinate-leak lint added (runtime egress check owed Phase 3/4). Verified: 21 unchanged rules byte-identical, only the 10 ratified rules changed, 30-cap held. Claimed no requirement authorises prescription — **corrected by the session-end reviewer (see ADR-0029 addendum): REQ-TIER-047/048/049 already authorise a recommendation below CONFIRMED with a disclosure contract**; `REQ-ACT` authoring opened for the *generation* machinery those requirements do not cover, reconciled-not-duplicated, blocked on OQ-30. Companion: `docs/CONSTITUTION_RESTRUCTURE_PROPOSAL.md` |

| ADR-0030 | Accepted | 2026-08-24 | **Ontology shapes for alcohol, mobility, strength — no migration, `0014` CHECK frozen.** Requirements-audit rulings (Joe). (1) Alcohol — and caffeine/supplements/medication — ride `kind='consume'` + a `metric_key` (`alcohol_standard_drinks`, `alcohol_ethanol_grams`), **not** a new `atoms.kind`; verified against 0002/0005/0014 that `consume` is a CHECK member, `atoms.metric_key` is a live FK, and no constraint ties kind→metric_key, so seeding is a registry data INSERT (deferred at the migration boundary). (2) Mobility scalars (radius of gyration, entropy, commute, transit, dwell, visit) are **`derived_measures`**, not atoms — designed-in, buildable only when that Phase-5 table exists (RULE-04 PENDING/OQ-22). (3) A strength set is **one `workout` atom per set** (exercise/load/reps/RPE), not per session — e1RM/volume/per-exercise trends need it; it is the objective function. Closes Missing-A + the ontology half of OQ-31; touches OQ-27. Spec edits + registry seeding owed in Track 1.2 |
| ADR-0031 | Accepted | 2026-08-24 | **Finance is a full system, net worth carved out, no live spend counter.** Requirements-audit ruling (Joe, C-3/Missing-C). IN (to be specified): income/earnings ingestion, account balances/cash position, budgets/targets, range-based forward forecasting, the `REQ-FIN-041` reconciliation-and-balance layer (currently a dangling reference). OUT for now: net worth, investments, portfolio (not a restraint call — just not asked; addable later). **Surviving constraint (does not reverse):** no live running counter of money spent/remaining — the $32–40 over-spend evidence is about precise always-on feedback, so budgets/forecasts are retrospective or range-based, never a live countdown. `REQ-FIN-210` KEPT (no-live-counter), `REQ-FIN-212` KEPT (range-only), `REQ-FIN-214` REVERSED (budgets IN; the old ban was per-category, so the reversal is knowingly broader). New surfaces must reconcile with RULE-23/24 (no scolding tally, no compliance score). Requirement-authoring only; no schema touched. Owed in Track 1.2 |

| ADR-0032 | Accepted | 2026-08-24 | **The EXPLORATORY surface is text and labels only — no charts.** Requirements-audit Missing-F (Joe's ruling). The RULE-17 SCOPE-shell surface that displays `CANDIDATE`/exploratory output under an `EXPLORATORY` label renders text and labels only — no scatter, trend line, edge/network diagram, heat map, or any graphical encoding. Reasoning: the whole risk RULE-17 guards is over-believing output that scored **at chance** (PCMCI+ AUROC ~0.52); a chart is the most persuasive format and makes a noise correlation look like a finding in a way a sentence never does — text forces the tier and uncertainty to be read, a scatter plot lets the reader skip them. REQ-NAR-035 (no chart for a `CANDIDATE` claim) stays coherent and unchanged; this extends it from one claim to the whole surface. **Revisit trigger (not silent):** once RULE-20 forward-prediction scoring has a track record on exploratory items, charts may be reconsidered *with that evidence*, not before. Implemented by REQ-TIER-050..052 (Missing-F). |
| ADR-0033 | Accepted | 2026-08-31 | **The alcohol `metric_registry` seed field values.** The two Missing-B rows (`alcohol_ethanol_grams`, `alcohol_standard_drinks`): `state_class='total'` (ADR-0030), `family='substance'`, `expected_cadence='irregular'`, `max_staleness_days=NULL` (an event total is not forward-filled), `plausible_low=0`, `plausible_high=NULL` (no evidence-based sanity ceiling — a guard may come at OQ-10 calibration), `self_report=false` (both keys are *derived* via volume×ABV, RULE-09, not coarsened self-report scales). `g_per_standard_drink` stays OQ-35 (a reference constant, not a column). A directly-logged drink count would get its own `self_report=true` key. Joe delegated these field choices ("finish it all"); recorded for audit, not preference. Applied as migration 0016. |
| ADR-0034 | Accepted | 2026-08-31 | **The capture ingress is a write-only RPC, not table access.** A Shortcut lands a capture by calling `public.ingest_capture(...)` via Supabase PostgREST — a `SECURITY DEFINER` function `anon` may EXECUTE and nothing else (no schema-`core` grant, no table grant). This is ADR-0020's read/egress separation applied to ingress: the public credential can append a self-authored capture and cannot read a row, reach `core`, touch another table, or mutate. `search_path=''` + fully-qualified names harden it; a source allow-list (`shortcut_*`/`pwa_text`) keeps third-party feeds off the public path; `trust_level` forced `trusted`; `capture_id` server-generated (0018) so the Shortcut needs no UUID. Proven in a disposable schema: anon RPC lands a capture, anon direct-INSERT denied, disallowed source rejected. Residual (named): capture-spam via a leaked anon key is bounded (append-only, write-only) and rate-limitable later. Needed no deploy and no agent-held secret. Migrations 0017 + 0018 (applied 2026-08-31, Joe authorised); Shortcut recipe in `docs/CAPTURE_SHORTCUT.md`. |
| ADR-0035 | Accepted | 2026-09-01 | **Check-ins bridge into the spine; deterministic extraction; hourly schedule.** An AFTER INSERT/UPDATE trigger on `public.checkins` mirrors every morning/night check-in submission (the phone keeps POSTing the OLD Edge Function unchanged) into `core.raw_captures` — zero device changes, zero new credentials; a guarded one-time mirror carries the existing real check-ins. Eleven `checkin_<type>_<field>` metric keys seeded with ADR-0018 coarsening (self_report, [0,10], 11 points). Extraction (`tools/extract_checkins.py`) is DETERMINISTIC — the fields were validated integers at ingest, so no model call, $0; per field one `self_report` atom with the coarsened interval [v−0.5,v+0.5], `estimate_method='self_report'`, subject_day per ADR-0019; notes become `note` atoms. Idempotent by atom existence (append-only `raw_captures` has no status to flip). Hourly GitHub Actions schedule (`extract.yml`). Proven end-to-end in a rolled-back transaction on the 3 REAL check-ins: 5 atoms, correct intervals/days, empty check-in → 0 atoms, re-run → 0, invariants ALL PASS. Live applies (0019, 0020) held for Joe's explicit yes per STANDING_RULINGS. Residuals named: empty check-ins re-scan (bounded); food fan-out stays old-system until the food shortcut; old/new self-report overlap is a Phase-5/6 reconciliation, not decided here. |

## Awaiting authorship

These are decisions already implied by the requirements and not yet written up.
Each must exist before the code it governs is written.

| ID | Decision to record |
|---|---|
| ADR-0003 | Evidence ladder — six tiers, permitted vocabulary per tier, promotion and demotion rules |
| ADR-0006 | Transaction ingestion — CSV/QFX plus Gmail parsing, `occurred_at` vs `posted_at`, dedupe key |
| ADR-0009 | Design tokens and honesty grammar — ⚠️ "carried forward from the archived UI system" premise is **void** (that archive was lost, OQ-19); must be **re-derived**, and this row re-scoped, at Phase 7 |

*Authored 2026-08-23 (Phase-2 session 2), moved to the accepted table above:
ADR-0004, 0007, 0008, 0010, 0016, 0017, 0018, 0019, 0020, 0021. ADR-0004 and
ADR-0008 were authored **partially** — ADR-0004 is the entity/link table shape
only (resolution algorithm still Phase 4); ADR-0008 is the capture schema only
(transport contract still Phase 3) — so each retains owed scope named in its file.*

**Note — ADR-0005 is a stub, not unwritten.** Its `weighed`-width decision is
written and accepted (see the top table and `docs/adr/0005-nutrition-resolution.md`).
Its remaining scope is still owed before Phase 3 and is deliberately kept out of
the table above so no row's status is self-contradictory: cache-first USDA
lookup, the personal portion table, the other method widths, and the count→grams
rule for branded items (REQ-NUT-050/051).
